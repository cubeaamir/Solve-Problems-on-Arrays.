class Solution
{
public:
	void pushZerosToEnd(int arr[], int n) 
  {
	    // code here
	    int j=-1;
	    int i=0;
	    j=i
      
	    for(int i=0;i<n;i++)
	    {
	        if(arr[i]!=0)
	        {
	            swap(arr[i],arr[j]);
	           j++;
	        }
	    }
	}
};
